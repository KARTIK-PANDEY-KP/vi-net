import React from 'react';

const AnimatedBackground = () => {
  return (
    <div className="fixed inset-0 -z-10 bg-white bottom-0 m-0 p-0">
      {/* Clean white background with no additional elements */}
    </div>
  );
};

export default AnimatedBackground;
